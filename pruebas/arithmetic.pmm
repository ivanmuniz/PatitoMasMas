programa arithmetic;
var float num1, num2;
string operacion;

funcion float sum(float num1, float num2)
{
    regresa(num1+num2);
}

principal() {
    escribe("Dime el primer numero");
    lee(num1);
    escribe("Dime el segundo numero");
    lee(num2);

    escribe("Que operacion quieres hacer (suma, resta, mult, div)");
    lee(operacion);

    si(operacion == "suma") entonces {
        escribe("El resultado de la suma es", sum(num1, num2));
    }
}